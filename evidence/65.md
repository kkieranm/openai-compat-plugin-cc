# OAI-65(d) — evidence

Moved out of the live OAI-65 item by the backlog sweep, 2026-08-17, because sub-item (d) is now
resolved while (a), (b) and (c) are not — so the whole item cannot go to `BACKLOG_DONE.md`, but the
resolved sub-item's evidence should not sit in the live body either. The original filing (verbatim,
moved before any correction) and the verification that closed it follow.

## Original filing, verbatim

**(d)** Deleting `jobs.db` makes a **new** job inherit an **old** job's log: `AUTOINCREMENT` restarts
at 1, `job-spawn.mjs:33` opens `'a'`, and `sweepQuietly` runs *after* `spawnWorker` so the new seq 1
is already a known row and its log is not an orphan. Observed end to end — `logs/1.log` survives
carrying era-1 output and is attributed to the new job, and both `/oai:status` and `/oai:result`
point the user at it.

## ALREADY FIXED, execution-verified 2026-08-17 by the sweep — no longer reproduces

OAI-67 (closed 2026-08-12) reordered submission so `sweepQuietly` now runs **before** `insertJob`
(`task-submit.mjs:253-256`), not after `spawnWorker` as the original filing describes. Reproduced the
exact scenario against current code: wrote a stale `logs/1.log`, deleted `jobs.db` so
`AUTOINCREMENT` restarts at 1, reopened and called `sweep(db)` in today's order — the stale log is
now collected as an orphan *before* the new seq-1 row exists, so `job-spawn.mjs`'s `'a'`-mode open
creates a fresh file.

**The residual is a race, not this mechanism**: a sweep listing the directory concurrently with a
second submission inserting the same seq is still open, and is now tracked more precisely as
[OAI-149](../BACKLOG.md), filed 2026-08-13 from the same subsystem and still unbuilt.
