# Backlog

Ordered; top item is next. IDs are stable and global (`LMS-n`, never reused).

- **LMS-1** — First feature: plugin skeleton + first working delegation command to an LM Studio model (scoped via /feature grilling).
